apiVersion: apps/v1
kind: Deployment
metadata:
  name: odoo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: odoo-app
  template:
    metadata:
      labels:
        app: odoo-app
    spec:
      containers:
        - name: odoo-app
          image: odoo:16
          ports:
            - containerPort: 8069
          env:
            - name: HOST
              value: postgres.local
            - name: PORT
              value: "30007"
            - name: POSTGRES_DB
              value: postgres
            - name: POSTGRES_USER
              value: odoo
            - name: POSTGRES_PASSWORD
              value: odoo
